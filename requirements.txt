# The simulator only needs pygame. Everything else is optional:
#   ROS 2 (rclpy + std/geometry/nav/sensor/rosgraph_msgs) — only for ROS mode.
#     Without ROS everything still runs; mecanum_lab/stub.py takes over the bus:
#     ./lab run --robot alice --controller student/controller_template.py
#   pytest — only for the tests (tests/).
#
# Ubuntu:  sudo apt install python3-pygame            (recommended, no venv needed)
# other:   pip3 install -r requirements.txt
pygame>=2.5
